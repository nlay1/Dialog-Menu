<Window x:Class="WpfApp21.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp21"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Menu IsMainMenu="True" Height="25" VerticalAlignment="Top"
              Background="Bisque">
            <MenuItem Header="File">
                <MenuItem Header="new...">
                    <MenuItem Header="Nikolay" Click="MenuItem_Click"></MenuItem>
                </MenuItem>
                <MenuItem Header="copy..." />
            </MenuItem>
            <MenuItem Header="Edit"></MenuItem>
        </Menu>
        <TextBox Name="nnn" Width="200" Height="100" Margin="0,25,0,0"
          VerticalAlignment="Top" ScrollViewer.HorizontalScrollBarVisibility="Visible">
            
        </TextBox>
        <ListBox Name="nnn1" Height="100" Width="200" HorizontalAlignment="Left" Margin="45,125,0,0" VerticalAlignment="Bottom" ScrollViewer.CanContentScroll="False"/>


    </Grid>
</Window>
